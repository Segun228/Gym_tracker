import {
    Panel,
} from '@vkontakte/vkui';

const WorkoutExercisePanel = ({id}) => {
    return ( 
    <Panel id={id}>
        <div>exact exercise</div>
    </Panel>);
}

export default WorkoutExercisePanel;