import {
    Panel,
} from '@vkontakte/vkui';

const ExactWorkoutPanel = ({ id }) => {
    return ( 
    <Panel id={id}>
        <div>exact workout</div>
    </Panel>);
}

export default ExactWorkoutPanel;