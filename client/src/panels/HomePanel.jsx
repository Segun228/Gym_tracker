import {
    CardGrid,
    Panel,
} from '@vkontakte/vkui';
import WorkoutCard from '../components/workoutCard/WorkoutCard';

const HomePanel = ({id}) => {
    return ( 
    <Panel id={id}>
    
    </Panel>);
}

export default HomePanel;